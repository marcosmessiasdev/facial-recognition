# Offline Demo assets

This folder bundles the same offline fixtures used by E2E tests, copied into the app output so the demo works without network access.

## Faces images

See `E2ETests/video/assets/THIRD_PARTY_NOTICES.md` in the repository for attribution of the face images (copied into `App/demo/assets/*`).

## Audio fixture

`App/demo/audio/marshall_plan_speech.wav` is copied from `E2ETests/audio/marshall_plan_speech.wav`.
See `E2ETests/audio/THIRD_PARTY_NOTICES.md` for attribution and licensing details.

